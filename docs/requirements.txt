# Dépendances de construction de la documentation (MkDocs Material).
# Utilisé par .github/workflows/docs.yml et pour un build local :
#   pip install -r docs/requirements.txt && mkdocs serve
#
# Ces deux versions se tiennent : mkdocs-material < 9.7.0 exige
# `pymdown-extensions~=10.2`, soit « >= 10.2 et < 11 ». Monter pymdown-extensions
# en 11 sans monter mkdocs-material rend l'installation insoluble. Les faire
# bouger ensemble, ou pas du tout.
mkdocs-material==9.7.7
pymdown-extensions==11.0.1
