<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>fr.univ_amu.iut</groupId>
    <artifactId>vigiechiro</artifactId>
    <name>VigieChiro Companion</name>

    <packaging>jar</packaging>
    <version>1.0-SNAPSHOT</version>
    <url>https://github.com/echonuit</url>

    <!-- Licence GPLv3 : conséquence de la dépendance Gluon Maps (carte interactive #152), elle-même GPL. -->
    <licenses>
        <license>
            <name>GNU General Public License v3.0</name>
            <url>https://www.gnu.org/licenses/gpl-3.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>

        <!-- Version inscrite au manifeste, et donc rendue par « À propos » et par l'option version
             de la CLI (#2108). Distincte de la balise version ci-dessus, qui reste 1.0-SNAPSHOT :
             semantic-release calcule la version au moment de publier et ne touche PAS au pom. Sans
             cette propriété, tout artefact publié annoncerait « 1.0-SNAPSHOT » : l'application
             mentirait sur elle-même précisément là où on l'interroge, dans un signalement d'anomalie.
             La CI la surcharge par app.version, comme elle le fait déjà pour jpackage.appVersion. -->
        <app.version>${project.version}</app.version>

        <audio.view.version>1.15.1</audio.view.version>

        <maven.compiler.plugin.version>3.15.0</maven.compiler.plugin.version>
        <maven.surefire.plugin.version>3.5.6</maven.surefire.plugin.version>
        <maven.shade.plugin.version>3.6.2</maven.shade.plugin.version>
        <maven.dependency.plugin.version>3.8.1</maven.dependency.plugin.version>
        <exec.maven.plugin.version>3.6.3</exec.maven.plugin.version>
        <spotless.maven.plugin.version>3.8.0</spotless.maven.plugin.version>
        <palantir.java.format.version>2.91.0</palantir.java.format.version>
        <git.build.hook.plugin.version>3.6.0</git.build.hook.plugin.version>
        <maven.pmd.plugin.version>3.28.0</maven.pmd.plugin.version>
        <javafx.version>26</javafx.version>
        <gluon.maps.version>2.0.0-ea+6</gluon.maps.version>
        <javafx.maven.plugin.version>0.0.8</javafx.maven.plugin.version>
        <javafx.mainClass>vigiechiro/fr.univ_amu.iut.App</javafx.mainClass>
        <testfx.version>4.0.18</testfx.version>

        <junit.jupiter.version>6.1.2</junit.jupiter.version>
        <assertj.version>3.27.7</assertj.version>
        <mockito.version>5.23.0</mockito.version>
        <slf4j.version>2.0.18</slf4j.version>
        <approvaltests.version>31.0.0</approvaltests.version>
        <archunit.version>1.4.2</archunit.version>
        <!-- Suite de contrat API « vivante » (profil -Papi-live, hors CI) : REST-assured tape l'API
             VigieChiro réelle et valide les réponses contre les JSON Schema de src/test/resources. -->
        <restassured.version>6.0.1</restassured.version>

        <!-- Lecture des specs YAML du générateur de cartes SD de recette (scope test uniquement,
             cf. recette/fixtures/spec/ ; jamais embarqué dans le binaire distribué). -->
        <snakeyaml.version>2.5</snakeyaml.version>

        <!-- Persistance + injection de dépendances (cf. TP5 / TP4). -->
        <guice.version>7.0.0</guice.version>
        <sqlite.jdbc.version>3.53.2.0</sqlite.jdbc.version>
        <!-- Sérialisation JSON des descripteurs de filtres (vues mémorisées, #623). -->
        <gson.version>2.14.0</gson.version>

        <!-- Interface en ligne de commande (socle de commandes, #614). -->
        <picocli.version>4.7.7</picocli.version>

        <!-- Icônes FontAwesome5 via Ikonli (FontIcon en FXML), disponible pour toutes les équipes. -->
        <ikonli.version>12.4.0</ikonli.version>

        <!-- Couverture (JaCoCo) + mutation (PIT, profil -Pmutation uniquement). -->
        <jacoco.version>0.8.14</jacoco.version>
        <pitest.version>1.25.8</pitest.version>
        <pitest.junit5.version>1.2.3</pitest.junit5.version>

        <!-- Rempli à la volée par jacoco:prepare-agent. Vide par défaut pour ne JAMAIS
             casser ./mvnw test (Surefire le réinjecte via @{jacocoArgLine}). -->
        <jacocoArgLine></jacocoArgLine>

        <!-- Seuil de couverture bloquant : false par défaut, true sous -Pquality-gate. -->
        <jacoco.haltOnFailure>false</jacoco.haltOnFailure>

        <!-- Sélection des tags JUnit 5 par Surefire, pilotée par propriété (override fiable en profil).
             Par défaut : on N'inclut aucun tag précis (tout tourne) SAUF `api-live`, exclu du build/CI.
             Le profil -Papi-live inverse ces deux propriétés pour ne lancer QUE la suite de contrat. -->
        <surefire.groups></surefire.groups>
        <surefire.excludedGroups>api-live</surefire.excludedGroups>

        <!-- Forks Surefire parallèles : une JVM par cœur (1C). Le toolkit JavaFX est un
             singleton PAR JVM : la parallélisation intra-JVM est impossible pour les tests
             TestFX, mais chaque fork démarre sa propre Headless Platform. Les tests sont
             isolés par @TempDir (aucune base SQLite ni port partagés). Revenir en séquentiel
             au besoin : ./mvnw test -Dsurefire.forkCount=1. -->
        <surefire.forkCount>1C</surefire.forkCount>

        <!-- Butoirs TestFX (#2120). Ce ne sont PAS des budgets de performance : ce sont des
             coupe-circuits d'interblocage, qui existent pour qu'un fil FX bloqué ne fige pas le
             build indéfiniment. Rien ne se mesure ici, on borne seulement une attente.

             Les valeurs par défaut de TestFX (30 s pour la mise en place d'un test, 60 s pour le
             démarrage du toolkit) supposent une JVM seule sur sa machine. Nous lançons une JVM par
             coeur (cf. surefire.forkCount ci-dessus) sur un runner partagé : ces deux décisions ont
             été prises séparément et étaient incohérentes.

             Mesure du 2026-07-22, 4 coeurs et 4 forks (la condition CI), machine au repos : la mise
             en place la plus lente de la suite est celle de SonsValidationViewTest#basculer_reference,
             à 6,9 s. Sous un butoir de 30 s, la marge n'était donc que de 4,3x. Or la durée du job
             `build` sur les 27 derniers runs va de 370 s à 2260 s : le runner varie d'un facteur
             5,4. Le butoir se trouvait DANS le bruit de la machine, d'où des expirations
             intermittentes sans qu'aucune assertion ne soit fausse.

             120 s laisse 17x de marge sur le nominal, soit 3x au-delà de la pire dégradation
             observée, tout en coupant un vrai interblocage en deux minutes. Garde-fou :
             ButoirsTestFxTest vérifie que ces valeurs atteignent bien la JVM forkée. -->
        <testfx.setup.timeout>120000</testfx.setup.timeout>
        <testfx.launch.timeout>120000</testfx.launch.timeout>
    </properties>

    <dependencies>
        <!-- Interface en ligne de commande : parseur d'arguments + sous-commandes (#614). -->
        <dependency>
            <groupId>info.picocli</groupId>
            <artifactId>picocli</artifactId>
            <version>${picocli.version}</version>
        </dependency>
        <dependency>
            <groupId>org.junit</groupId>
            <artifactId>junit-bom</artifactId>
            <version>${junit.jupiter.version}</version>
            <type>pom</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.approvaltests</groupId>
            <artifactId>approvaltests</artifactId>
            <version>${approvaltests.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- Backend SLF4J « no-op » côté test : des libs de test tirent slf4j-api sans binding, d'où le
             « No SLF4J providers were found » répété à chaque fork. slf4j-nop fournit un backend
             silencieux (logs ignorés, comme le comportement par défaut actuel). Test uniquement. -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-nop</artifactId>
            <version>${slf4j.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-media</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <!-- javafx-swing : SwingFXUtils pour la capture d'écran hors-écran (outil de capture,
             cf. commun.outils.ApercuFx). Version alignée sur le reste de JavaFX. -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-swing</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.testfx</groupId>
            <artifactId>testfx-core</artifactId>
            <version>${testfx.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testfx</groupId>
            <artifactId>testfx-junit5</artifactId>
            <version>${testfx.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>fr.nedjar.vigiechiro</groupId>
            <artifactId>audio-view</artifactId>
            <version>${audio.view.version}</version>
        </dependency>
        <!-- Carte interactive (#152) : Gluon Maps (MapView + MapLayer). ATTENTION licence GPL :
             le dépôt passe en GPL en conséquence. -->
        <dependency>
            <groupId>com.gluonhq</groupId>
            <artifactId>maps</artifactId>
            <version>${gluon.maps.version}</version>
        </dependency>
        <!-- Persistance JDBC : driver SQLite (module automatique org.xerial.sqlitejdbc). -->
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>${sqlite.jdbc.version}</version>
        </dependency>

        <!-- Injection de dépendances (Guice 7), comme au TP4. -->
        <dependency>
            <groupId>com.google.inject</groupId>
            <artifactId>guice</artifactId>
            <version>${guice.version}</version>
        </dependency>

        <!-- Sérialisation JSON (Gson) : (dé)sérialisation des descripteurs de filtres des vues
             mémorisées (#623). Module automatique `com.google.gson` ; supporte nativement les records. -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
        </dependency>

        <!-- ArchUnit : tests d'architecture (règles de couches / absence de cycles). -->
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <version>${archunit.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- REST-assured : suite de contrat API vivante (@Tag("api-live"), profil -Papi-live, JAMAIS en
             CI ni dans le build par défaut, cf. surefire excludedGroups). Assertions fluides sur le JSON
             brut de l'API réelle + validation contre les JSON Schema partagés (json-schema-validator). -->
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <version>${restassured.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>json-schema-validator</artifactId>
            <version>${restassured.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Générateur déterministe de cartes SD de recette : lit les specs YAML de
             recette/fixtures/spec/. Test uniquement (l'application n'embarque pas la génération de
             fixtures ; ce sont les tests qui fabriquent WAV/ZIP, cf. paquet de test recette). -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>${snakeyaml.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- Icônes FontAwesome5 via Ikonli (FontIcon en FXML). ikonli-javafx tire ikonli-core. -->
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-javafx</artifactId>
            <version>${ikonli.version}</version>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-fontawesome5-pack</artifactId>
            <version>${ikonli.version}</version>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>**/*</include>
                </includes>
            </resource>
            <!--
              Convention TP3 : les .fxml/.css de vue vivent À CÔTÉ de leur controller
              dans src/main/java/.../view/, sans naviguer entre src/main/java et
              src/main/resources. Tous les fichiers non-Java de src/main/java sont
              copiés dans target/classes au même chemin de paquetage (les ressources
              PARTAGÉES (db/migration, DarkTheme.css) restent dans src/main/resources).
            -->
            <resource>
                <directory>src/main/java</directory>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </resource>
        </resources>

        <plugins>
            <!-- ========== Compilation & ressources (FXML/CSS co-localisés dans src/main/java) ========== -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <!-- Recopie les ressources (FXML/CSS co-localisées dans src/main/java) APRÈS la
                         compilation. En incrémental, le compilateur vide `target/classes` dès qu'il détecte
                         des fichiers source ajoutés/supprimés (typiquement après un `git pull`), effaçant
                         les FXML copiés en `process-resources` : `./mvnw javafx:run` échouait alors sur
                         « Location is not set » (MainView.fxml introuvable). Cette copie en `process-classes`
                         (après `compile`, avant l'exécution/le packaging) les restaure, sans imposer de
                         `clean`. Le but `resources` réutilise le `<resources>` du projet, sans duplication. -->
                    <execution>
                        <id>recopier-ressources-apres-compilation</id>
                        <phase>process-classes</phase>
                        <goals>
                            <goal>resources</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- ========== Exécution (exec:java) & packaging du fat-jar (shade) ========== -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>${exec.maven.plugin.version}</version>
                <configuration>
                    <mainClass>fr.univ_amu.iut.App</mainClass>
                </configuration>
                <executions>
                    <!-- Goal de confort (non lié à une phase, jamais exécuté en CI ni au packaging) :
                         matérialise les cartes SD de recette depuis leurs specs YAML (portée test).
                         Lancer via :
                           ./mvnw -q test-compile exec:java@generer-sd \
                             -Dexec.args="recette/fixtures/spec <dossierDest>"
                         cf. dev-docs/recette/fixtures.md. -->
                    <execution>
                        <id>generer-sd</id>
                        <configuration>
                            <mainClass>fr.univ_amu.iut.recette.GenerateurCartesSD</mainClass>
                            <classpathScope>test</classpathScope>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Manifeste du jar MINCE : y inscrit Implementation-Title et Implementation-Version,
                 que `Package.getImplementationVersion()` lit à l'exécution. Sans cela l'application
                 ignore sa propre version (#2108) - un utilisateur qui signale une anomalie ne peut
                 pas dire laquelle il utilise, et aucune vérification de mise à jour n'est possible.
                 Le fat-jar, lui, reconstruit son manifeste : cf. le transformer du shade ci-dessous. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Implementation-Title>${project.name}</Implementation-Title>
                            <Implementation-Version>${app.version}</Implementation-Version>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>${maven.shade.plugin.version}</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <filters>
                                <!-- Les module-info.class des dépendances n'ont aucun sens dans un
                                     fat-jar chargé en classpath (pas de module path) et le shade
                                     avertit qu'il « casse leur encapsulation forte ». On les retire,
                                     y compris les variantes multi-release (META-INF/versions/N/). -->
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>module-info.class</exclude>
                                        <exclude>META-INF/versions/*/module-info.class</exclude>
                                        <!-- Signatures des jars signés : invalides une fois fusionnées. -->
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <!-- Config GraalVM native-image (reflect-config Ikonli…) :
                                             inutile en JVM classique, et en double entre 3 jars Ikonli. -->
                                        <exclude>META-INF/native-image/**</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <!-- Main-Class = Launcher (et non App, qui étend Application) : le
                                     fat-jar est ainsi lançable directement (java -jar) sans l'erreur
                                     « JavaFX runtime components are missing ». -->
                                <transformer
                                    implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>fr.univ_amu.iut.Launcher</mainClass>
                                    <!-- Le fat-jar a son PROPRE manifeste, reconstruit par le shade :
                                         les entrées du jar mince n'y arrivent pas toutes seules. Sans
                                         cela, l'application distribuée ignore sa version alors que le
                                         jar mince la connaît - or c'est le fat-jar qui est empaqueté
                                         par jpackage, donc celui que l'utilisateur exécute (#2108). -->
                                    <manifestEntries>
                                        <Implementation-Title>${project.name}</Implementation-Title>
                                        <Implementation-Version>${app.version}</Implementation-Version>
                                    </manifestEntries>
                                </transformer>
                                <!-- Fusionne les META-INF/services de toutes les dépendances (sinon
                                     les fournisseurs Ikonli FontAwesome sont perdus -> icônes fas-*
                                     manquantes et FXML cassés au chargement). -->
                                <transformer
                                    implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <!-- Attache le fat-jar sous le classifier `shaded` au lieu de REMPLACER
                                 l'artefact principal (#1188). `vigiechiro-*.jar` reste donc le jar MINCE
                                 (sortie du jar-plugin), et le fat-jar devient `vigiechiro-*-shaded.jar`.
                                 Conséquence : le shade ne consomme plus jamais sa propre sortie en build
                                 incrémental (packaging idempotent), et le `target/original-*.jar` (le jar
                                 pré-shade renommé) n'existe plus. Le profil `installer` (jpackage) et le
                                 smoke-test CI pointent sur le classifier `shaded`. -->
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>shaded</shadedClassifierName>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Expose le chemin des jars de dépendances comme propriétés
                 (${groupId:artifactId:type}), pour passer byte-buddy-agent en -javaagent à Surefire
                 (agent Mockito explicite, cf. argLine ci-dessous). Lié à la phase `initialize`. -->
            <!-- ========== Tests : agent Mockito (chemin exposé) + Surefire headless (TestFX) ========== -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>${maven.dependency.plugin.version}</version>
                <executions>
                    <execution>
                        <id>exposer-chemins-dependances</id>
                        <goals>
                            <goal>properties</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.plugin.version}</version>
                <configuration>
                    <!-- TestFX requires classpath mode (not module path) -->
                    <useModulePath>false</useModulePath>
                    <!-- Répartit les CLASSES de test sur plusieurs JVM (cf. propriété
                         surefire.forkCount). JaCoCo agrège les forks dans le même jacoco.exec
                         (mode append de l'agent). -->
                    <forkCount>${surefire.forkCount}</forkCount>
                    <reuseForks>true</reuseForks>
                    <!-- La suite de contrat API (@Tag("api-live")) tape l'API VigieChiro réelle : JAMAIS
                         dans le build/CI par défaut (surefire.excludedGroups=api-live). On la lance à la
                         main via le profil -Papi-live, qui inverse ces deux propriétés. -->
                    <groups>${surefire.groups}</groups>
                    <excludedGroups>${surefire.excludedGroups}</excludedGroups>
                    <!-- Headless Platform JavaFX 26 (Gluon) : plateforme Glass logicielle, sans
                         natif ni serveur d'affichage. Aucune fenêtre TestFX/JavaFX n'apparaît à
                         l'écran, localement comme en CI, et plus besoin de xvfb. Le robot Glass
                         pilote la plateforme Headless, le pipeline de rendu logiciel (sw)
                         remplace le GPU. Remplace l'ancien bricolage Monocle.
                         NB : on NE met PAS testfx.headless=true : ce flag déclencherait le vieux
                         bootstrap Monocle de TestFX (recherche de MonoclePlatformFactory, classe
                         absente sans la dépendance monocle). Le headless vient ici de
                         glass.platform=Headless, pas de TestFX. -->
                    <systemPropertyVariables>
                        <glass.platform>Headless</glass.platform>
                        <prism.order>sw</prism.order>
                        <java.awt.headless>true</java.awt.headless>
                        <testfx.robot>glass</testfx.robot>
                        <!-- Coupe-circuits d'interblocage TestFX, calibrés en propriétés plus haut
                             (#2120). TestFX les lit via Long.getLong au premier FxToolkitContext :
                             ils doivent donc être posés sur la JVM FORKÉE, pas sur celle de Maven. -->
                        <testfx.setup.timeout>${testfx.setup.timeout}</testfx.setup.timeout>
                        <testfx.launch.timeout>${testfx.launch.timeout}</testfx.launch.timeout>
                        <!-- Config JUL de test : coupe le logger JournalisationTache, dont les SEVERE
                             + traces (voulus en prod, #1538) sont NORMAUX quand un test exerce un chemin
                             d'échec et noieraient le journal de CI. Prod inchangée (App/Cli installent
                             leur propre config). Cf. src/test/resources/logging-tests.properties. -->
                        <java.util.logging.config.file>${project.basedir}/src/test/resources/logging-tests.properties</java.util.logging.config.file>
                        <!-- Configuration d'amorçage (#1038) détournée vers target/ : sans cela, la suite
                             lirait celle de qui lance le build, et passerait ou échouerait selon la machine.
                             Le dossier n'a pas besoin d'exister : une configuration absente est vide. -->
                        <vigiechiro.config>${project.build.directory}/config-amorcage-test</vigiechiro.config>
                    </systemPropertyVariables>
                    <!-- Tests en mode CLASSPATH (useModulePath=false, requis par TestFX) : JavaFX est
                         chargé dans le module SANS NOM, il n'existe donc aucun module nommé `javafx.graphics`
                         ou `javafx.base`. Les options `add-reads`/`add-opens`/`add-exports` visant ces
                         modules (et `enable-native-access=javafx.graphics`) ciblaient des modules
                         inexistants ici : la JVM les rejetait (« Unknown module: javafx.graphics »), pur
                         bruit sans effet. Le headless vient de `glass.platform=Headless`, pas de ces
                         options. On ne garde que ce qui sert réellement en classpath. -->
                    <argLine>
                        @{jacocoArgLine}
                        -javaagent:${net.bytebuddy:byte-buddy-agent:jar}
                        --enable-native-access=ALL-UNNAMED
                    </argLine>
                </configuration>
            </plugin>

            <!-- Spotless : formatage Palantir Java Format. Pas de binding Maven : appliqué via
                 ./mvnw spotless:apply (hook pre-commit) et vérifié en CI par spotless:check (lint.yml). -->
            <!-- ========== Qualité : format (Spotless), hook pre-commit, PMD, couverture (JaCoCo) ========== -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.maven.plugin.version}</version>
                <configuration>
                    <java>
                        <palantirJavaFormat>
                            <version>${palantir.java.format.version}</version>
                            <style>PALANTIR</style>
                            <!-- formatJavadoc=false : on préserve les doc-comments Markdown
                                 Java (JEP 467, ///) sans que Palantir les reformate. -->
                            <formatJavadoc>false</formatJavadoc>
                        </palantirJavaFormat>
                        <importOrder />
                        <removeUnusedImports />
                        <formatAnnotations />
                        <trimTrailingWhitespace />
                        <endWithNewline />
                    </java>
                </configuration>
            </plugin>

            <!-- git-build-hook : configure silencieusement
                 core.hooksPath=.githooks au premier ./mvnw. -->
            <plugin>
                <groupId>com.rudikershaw.gitbuildhook</groupId>
                <artifactId>git-build-hook-maven-plugin</artifactId>
                <version>${git.build.hook.plugin.version}</version>
                <configuration>
                    <gitConfig>
                        <core.hooksPath>.githooks</core.hooksPath>
                    </gitConfig>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>configure</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- PMD : linter de code smells aligné sur les refactorings
                 enseignés. Ruleset pédagogique dans pmd-ruleset.xml.
                 Par défaut failOnViolation=false (warning only) ; le
                 profil -Pquality-gate active failOnViolation=true. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
                <version>${maven.pmd.plugin.version}</version>
                <configuration>
                    <rulesets>
                        <ruleset>${project.basedir}/pmd-ruleset.xml</ruleset>
                    </rulesets>
                    <printFailingErrors>true</printFailingErrors>
                    <failOnViolation>false</failOnViolation>
                    <linkXRef>false</linkXRef>
                    <targetJdk>25</targetJdk>
                    <includeTests>false</includeTests>
                </configuration>
                <executions>
                    <execution>
                        <id>pmd-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!-- JaCoCo : couverture de tests. Le `check` porte des seuils (85 % lignes /
                 70 % branches au niveau BUNDLE) rendus BLOQUANTS en CI par maven.yml
                 (`-Djacoco.haltOnFailure=true`). En build local nu ils restent tolérants
                 (haltOnFailure=false par défaut, cf. propriété plus bas) : la CI fait foi. -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                        <configuration>
                            <propertyName>jacocoArgLine</propertyName>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <excludes>
                                <exclude>**/outils/**</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <!-- Bloquant uniquement sous -Pquality-gate, comme PMD :
                                 un ./mvnw verify nu reporte sans casser le build. -->
                            <haltOnFailure>${jacoco.haltOnFailure}</haltOnFailure>
                            <!-- Les outils (capture d'écran, bancs de mesure) sont validés par
                                 exécution, pas par des tests unitaires : exclus du calcul de
                                 couverture (sinon ils la tireraient artificiellement vers le bas). -->
                            <excludes>
                                <exclude>**/outils/**</exclude>
                            </excludes>
                            <!-- D'où viennent ces deux valeurs (76dc24cf, 2026-06-04) : chacune a été
                                 posée environ 4 points SOUS la couverture mesurée ce jour-là (89,6 %
                                 de lignes, 74 % de branches). Le seuil des branches est plus bas que
                                 celui des lignes parce que la couverture des branches l'était déjà,
                                 pas par convention. L'écart volontaire devait servir de cliquet, à
                                 resserrer à mesure que la couverture monte.
                                 Premier resserrage, 2026-07-22 : la couverture avait atteint 90,91 %
                                 de lignes et 78,75 % de branches, laissant 5,9 et 8,8 points de jeu.
                                 Les seuils passent de 85/70 à 88/76, soit à nouveau ~3 points sous la
                                 mesure. Le jeu résiduel n'est pas une négligence : il absorbe la
                                 variation normale d'une PR qui touche du code peu testable, sans quoi
                                 la CI rougirait sans régression réelle.
                                 Re-mesurer avant de toucher à ces chiffres : ./mvnw verify, puis
                                 target/site/jacoco/jacoco.csv, LINE_COVERED / (COVERED + MISSED).
                                 Le rapport applique déjà l'exclusion **/outils/** ci-dessus, ses
                                 ratios sont donc directement ceux que ce portail compare. -->
                            <rules>
                                <rule>
                                    <element>BUNDLE</element>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.88</minimum>
                                        </limit>
                                        <limit>
                                            <counter>BRANCH</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.76</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- ========== Lancement de l'application (./mvnw javafx:run) ========== -->
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>${javafx.maven.plugin.version}</version>
                <configuration>
                    <jlinkImageName>app</jlinkImageName>
                    <launcher>launcher</launcher>
                    <mainClass>${javafx.mainClass}</mainClass>
                    <options>
                        <!-- Native access (Java 25 strict) : JavaFX + driver natif SQLite (JNI).
                             Sans la ligne sqlitejdbc, le premier accès base affiche le WARNING
                             « restricted method java.lang.System::load ... ». -->
                        <option>--enable-native-access=javafx.graphics</option>
                        <option>--enable-native-access=org.xerial.sqlitejdbc</option>
                        <!-- Neutralise le WARNING sun.misc.Unsafe de Guice/Guava (JEP 498). -->
                        <option>--sun-misc-unsafe-memory-access=allow</option>
                    </options>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- ========== Profils (hors build par défaut) : installer / jpackage-mac (installeurs natifs
             jpackage), mutation (tests PIT), quality-gate (PMD + JaCoCo bloquants), api-live (contrat
             REST-assured contre l'API réelle, hors CI) ========== -->
        <!-- Profil installer : construit un installeur natif (jpackage) à partir du fat-jar shade.
             jlink modulaire est IMPOSSIBLE ici : Guice 7 et sa grappe transitive (Guava, aopalliance,
             jsr305...) sont des modules AUTOMATIQUES, refusés par jlink (sqlite-jdbc, lui, est un vrai
             module). On empaquette donc le fat-jar (qui embarque déjà JavaFX + ses natifs + toutes les
             dépendances) en mode classpath, avec une classe Launcher (non-Application) et un runtime
             composé UNIQUEMENT de modules JDK nommés (via l'option add-modules) : les modules
             automatiques restent sur le classpath, hors résolution jlink.
             Bindé sur la phase verify (APRÈS shade : sur package, jpackage empaquetterait le jar mince).
             Usage : ./mvnw -Pinstaller -Djpackage.type=deb -DskipTests verify
             Type par OS : deb/rpm (Linux), dmg/pkg (macOS), msi/exe (Windows). Sortie : target/dist/. -->
        <profile>
            <id>installer</id>
            <properties>
                <jpackage.type>deb</jpackage.type>
                <jpackage.appVersion>1.0.0</jpackage.appVersion>
                <!-- Icône par défaut (Linux) ; les profils jpackage-mac et jpackage-windows,
                     déclarés APRÈS celui-ci, la remplacent par le format attendu de leur système.
                     Sans l'option icon, jpackage retombe sur son icône Duke générique (#2144). -->
                <jpackage.icon>${project.basedir}/icone/derive/vigiechiro.png</jpackage.icon>
            </properties>
            <build>
                <plugins>
                    <!-- 1. Isole le fat-jar dans un dossier d'entrée propre (jpackage embarque tout
                         le contenu du dossier d'entrée ; on ne veut QUE le fat-jar, pas les classes). -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.5.0</version>
                        <executions>
                            <execution>
                                <id>jpackage-input</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.build.directory}</directory>
                                            <includes>
                                                <include>${project.build.finalName}-shaded.jar</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- 2. Appelle l'outil jpackage du JDK (${java.home}/bin/jpackage). -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>jpackage</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>${java.home}/bin/jpackage</executable>
                                    <arguments>
                                        <argument>--type</argument>
                                        <argument>${jpackage.type}</argument>
                                        <argument>--name</argument>
                                        <argument>VigieChiroCompanion</argument>
                                        <argument>--app-version</argument>
                                        <argument>${jpackage.appVersion}</argument>
                                        <!-- Description : sans elle, jpackage recopie le NOM dans le
                                             champ Comment du .desktop, et l'infobulle du menu répète
                                             « VigieChiro » au lieu de dire ce que fait le logiciel
                                             (#2180). -->
                                        <argument>--description</argument>
                                        <argument>Préparer et déposer les nuits d'enregistrement de chiroptères</argument>
                                        <argument>--vendor</argument>
                                        <!-- Éditeur du produit, affiché dans les propriétés de
                                             l'installeur Windows et les métadonnées du paquet
                                             Debian. C'est le PROJET (Echonuit), non l'établissement
                                             ni la personne : ce dépôt est né d'une SAÉ mais n'en est
                                             plus le dépôt (#2108), et le projet a désormais une
                                             identité propre (ADR 0047). L'auteur, Sébastien Nedjar,
                                             est crédité dans le README. -->
                                        <argument>Echonuit</argument>
                                        <argument>--input</argument>
                                        <argument>${project.build.directory}/jpackage-input</argument>
                                        <argument>--main-jar</argument>
                                        <argument>${project.build.finalName}-shaded.jar</argument>
                                        <argument>--main-class</argument>
                                        <argument>fr.univ_amu.iut.Launcher</argument>
                                        <argument>--icon</argument>
                                        <argument>${jpackage.icon}</argument>
                                        <!-- Accès natif (JavaFX + JNI SQLite) et Unsafe (Guice/Guava)
                                             autorisés, comme pour javafx:run : sinon avertissements,
                                             puis blocage dans un futur JDK. -->
                                        <argument>--java-options</argument>
                                        <argument>--enable-native-access=ALL-UNNAMED</argument>
                                        <argument>--java-options</argument>
                                        <argument>--sun-misc-unsafe-memory-access=allow</argument>
                                        <!-- Runtime : modules JDK nommés uniquement (JavaFX vient du
                                             fat-jar, sur le classpath).
                                             Cette liste est le SEUL contrat entre le code et le runtime
                                             livré : le fat-jar s'exécute sur le classpath, donc
                                             module-info.java est ignoré à l'exécution et ne compense
                                             RIEN d'oublié ici. Un module absent ne casse pas le build,
                                             il casse l'application chez l'utilisateur, à l'instant où
                                             elle touche la classe manquante.
                                             C'est arrivé : java.net.http est entré dans le code début
                                             juillet 2026 (clients Open-Meteo puis Vigie-Chiro) sans être
                                             ajouté ici, et tous les paquets ont démarré puis planté sur
                                             NoClassDefFoundError HttpClient - message affiché « Failed
                                             to launch JVM » sous Windows, où le lanceur jpackage n'a pas
                                             de console pour montrer la trace (#2299).
                                             La liste se recalcule en passant le fat-jar à `jdeps` avec
                                             ses options print-module-deps et ignore-missing-deps, et le
                                             job `paquet` DÉMARRE le paquet construit pour vérifier que
                                             cette liste suffit vraiment. -->
                                        <argument>--add-modules</argument>
                                        <argument>java.base,java.desktop,java.logging,java.net.http,java.sql,java.naming,java.scripting,jdk.crypto.ec,jdk.jfr,jdk.localedata,jdk.unsupported,jdk.unsupported.desktop</argument>
                                        <argument>--dest</argument>
                                        <argument>${project.build.directory}/dist</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Profil jpackage-mac : activé automatiquement sur macOS. Sur cette plateforme, jpackage
             exige un bundle identifier valide (lettres/chiffres/tirets/points, PAS d'underscore) ;
             le défaut dérivé du package Java `fr.univ_amu.iut` est rejeté (« invalid mac bundle
             identifier »). On injecte donc l'option mac-package-identifier (valide uniquement sur
             macOS, d'où l'activation par OS). L'exécution `jpackage` (sans phase ni goals propres)
             se fusionne avec celle du profil `installer` ; combine.children=append ajoute ces deux
             arguments à la fin de la liste. Inerte hors profil installer (rien à empaqueter). -->
        <!-- Profil jpackage-linux : activé automatiquement sous Linux, pour donner à l'entrée de
             menu une catégorie VALIDE. Sans cette option, jpackage écrit `Categories=Unknown` dans le
             .desktop du paquet : valeur que `desktop-file-validate` refuse, et qui range l'application
             nulle part dans le menu des environnements de bureau (#2180, trouvé en empaquetant le
             Flatpak, qui échouait sur ce fichier).
             L'option linux-app-category est autre chose : c'est la Section du paquet Debian, pas
             la catégorie du menu. Les deux sont renseignées, elles ne se remplacent pas.
             Déclaré après le profil `installer`, dont il complète les arguments. -->
        <profile>
            <id>jpackage-linux</id>
            <activation>
                <os>
                    <name>Linux</name>
                </os>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>jpackage</id>
                                <configuration>
                                    <arguments combine.children="append">
                                        <argument>--linux-menu-group</argument>
                                        <argument>Science;Biology;</argument>
                                        <argument>--linux-app-category</argument>
                                        <argument>science</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Profil jpackage-windows : activé automatiquement sous Windows. Il change le format d'icône
             (Windows exige un ICO, non un PNG, sans quoi jpackage échoue) ET complète l'appel jpackage
             avec les options propres à l'installeur MSI (voir ADR 0045). Déclaré après le profil
             `installer`, dont il remplace la propriété d'icône et dont il APPEND les arguments
             (combine.children="append", même patron que jpackage-linux). Inerte hors profil installer
             (aucun empaquetage à faire). -->
        <profile>
            <id>jpackage-windows</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <properties>
                <jpackage.icon>${project.basedir}/icone/derive/vigiechiro.ico</jpackage.icon>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>jpackage</id>
                                <configuration>
                                    <arguments combine.children="append">
                                        <!-- UpgradeCode FIGÉ. Sans cette option, jpackage tire un UUID
                                             ALÉATOIRE à chaque build (JDK-8214564) : chaque version
                                             devient un produit Windows distinct, et `winget upgrade`
                                             installerait la nouvelle À CÔTÉ de l'ancienne au lieu de la
                                             remplacer. Ce GUID est une CONSTANTE d'identité : ne jamais
                                             le régénérer (cf. ADR 0045). Il déclenche aussi l'élément
                                             WiX <MajorUpgrade> qui retire l'ancienne version. -->
                                        <argument>--win-upgrade-uuid</argument>
                                        <argument>0328d083-bdf7-4e84-95bf-918249478c00</argument>
                                        <!-- Installation PER-USER (%LOCALAPPDATA%), sans UAC ni droits
                                             administrateur : le public visé est un observateur de
                                             terrain sur un poste institutionnel non-administrateur. Le
                                             scope fait partie de l'identité vue par winget, il se fige
                                             donc en même temps que l'UpgradeCode. -->
                                        <argument>--win-per-user-install</argument>
                                        <!-- Entrée dans le menu Démarrer : sans elle, l'installeur
                                             dépose les fichiers mais n'offre aucun moyen de lancer
                                             l'application. -->
                                        <argument>--win-menu</argument>
                                        <argument>--win-menu-group</argument>
                                        <argument>VigieChiro Companion</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>jpackage-mac</id>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <properties>
                <!-- macOS exige un ICNS. Cf. icone/ecrit-icns.py : ImageMagick écrit un PNG déguisé
                     sous ce nom, que jpackage refuse. -->
                <jpackage.icon>${project.basedir}/icone/derive/vigiechiro.icns</jpackage.icon>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>jpackage</id>
                                <configuration>
                                    <arguments combine.children="append">
                                        <argument>--mac-package-identifier</argument>
                                        <argument>fr.echonuit.VigieChiroCompanion</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Profil mutation : PIT (org.pitest). NON exécuté par défaut.
             Lancer explicitement via ./mvnw -Pmutation test (lent). -->
        <profile>
            <id>mutation</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.pitest</groupId>
                        <artifactId>pitest-maven</artifactId>
                        <version>${pitest.version}</version>
                        <configuration>
                            <targetClasses>
                                <param>fr.univ_amu.iut.*</param>
                            </targetClasses>
                            <targetTests>
                                <param>fr.univ_amu.iut.*</param>
                            </targetTests>
                            <outputFormats>
                                <param>HTML</param>
                                <param>XML</param>
                            </outputFormats>
                            <timestampedReports>false</timestampedReports>
                            <!-- PIT hérite de l'argLine de surefire, mais PAS de ses systemPropertyVariables :
                                 sans ces quatre propriétés, le minion démarre JavaFX hors headless, TestFX
                                 échoue, et PIT abandonne (« requires a green suite »). Une classe de vue
                                 devenait ainsi impossible à mutation-tester. Reproduit ici pour le minion. -->
                            <jvmArgs>
                                <jvmArg>-Dglass.platform=Headless</jvmArg>
                                <jvmArg>-Dprism.order=sw</jvmArg>
                                <jvmArg>-Djava.awt.headless=true</jvmArg>
                                <jvmArg>-Dtestfx.robot=glass</jvmArg>
                            </jvmArgs>
                        </configuration>
                        <dependencies>
                            <dependency>
                                <groupId>org.pitest</groupId>
                                <artifactId>pitest-junit5-plugin</artifactId>
                                <version>${pitest.junit5.version}</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Profil portail qualité : PMD bloque sur violation. -->
        <profile>
            <id>quality-gate</id>
            <properties>
                <!-- Rend le seuil de couverture JaCoCo bloquant. -->
                <jacoco.haltOnFailure>true</jacoco.haltOnFailure>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-pmd-plugin</artifactId>
                        <configuration>
                            <failOnViolation>true</failOnViolation>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Profil api-live : lance UNIQUEMENT la suite de contrat API (@Tag("api-live")), qui tape
             l'API VigieChiro réelle. Hors CI, à la main :
               ./mvnw -Papi-live test -Dvigiechiro.token=XXXX                     (lecture seule, sûr)
               ./mvnw -Papi-live test -Dvigiechiro.token=XXXX -Dvigiechiro.write=true  (+ probes d'écriture)
             Sans token, les tests se SKIPPENT proprement (Assumptions). Le token/baseUrl/write sont
             transmis au JVM de test via systemPropertyVariables (valeurs par défaut ci-dessous). -->
        <profile>
            <id>api-live</id>
            <properties>
                <!-- Inverse la sélection : ne lance QUE @Tag("api-live"), sans exclusion. -->
                <surefire.groups>api-live</surefire.groups>
                <surefire.excludedGroups></surefire.excludedGroups>
                <vigiechiro.token></vigiechiro.token>
                <vigiechiro.baseUrl>https://vigiechiro.herokuapp.com/api/v1</vigiechiro.baseUrl>
                <vigiechiro.write>false</vigiechiro.write>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <!-- Transmet token/baseUrl/write au JVM de test (la sélection des tags est
                                 pilotée par les propriétés surefire.groups/excludedGroups ci-dessus). -->
                            <systemPropertyVariables>
                                <vigiechiro.token>${vigiechiro.token}</vigiechiro.token>
                                <vigiechiro.baseUrl>${vigiechiro.baseUrl}</vigiechiro.baseUrl>
                                <vigiechiro.write>${vigiechiro.write}</vigiechiro.write>
                            </systemPropertyVariables>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>