<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>
<?import org.kordamp.ikonli.javafx.FontIcon?>

<!--
  M-Passage - écran pivot d'un passage (détail), en « hub à plat » : bandeau d'identité, stepper de
  statut workflow, résumé de la nuit (stats) et un jeu de cartes d'actions « avancer » (Vérifier,
  Diagnostic, Préparer le dépôt, Validation Tadarida). Le retour et le fil d'Ariane sont portés par le
  chrome (commun) : pas de fil interne ni d'onglets-lanceurs ici. Les cartes ouvrent M-Qualification,
  M-Diagnostic, M-Lot et M-Vision-Tadarida via les contrats socle (Ouvrir*). Pur câblage : tout est
  lié au PassageViewModel par PassageController.
-->
<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:id="racine"
            fx:controller="fr.univ_amu.iut.passage.view.PassageController"
            stylesheets="@../../commun/view/palette.css, @../../commun/view/design.css, @passage.css"
            styleClass="ecran-passage">

  <top>
    <VBox spacing="12.0" styleClass="entete">
      <HBox alignment="CENTER_LEFT" spacing="12.0">
        <Label fx:id="lblTitre" styleClass="titre-page" maxWidth="Infinity" HBox.hgrow="ALWAYS"/>
        <Button fx:id="boutonVoirCarte" text="🗺 Voir sur la carte"
                styleClass="action-secondaire" onAction="#voirSurCarte"/>
        <Button fx:id="boutonRattachement" text="✏ Modifier rattachement"
                styleClass="action-secondaire" onAction="#modifierRattachement"/>
        <Button fx:id="boutonAnnulerDepot" text="↩ Annuler le dépôt"
                styleClass="action-secondaire" onAction="#annulerDepot"/>
        <Button fx:id="boutonPurger" text="🧹 Purger les originaux"
                styleClass="action-secondaire" onAction="#purgerOriginaux"
                visible="false" managed="false"/>
        <Button fx:id="boutonSupprimer" text="🗑 Supprimer" styleClass="bouton-danger"
                onAction="#supprimer"/>
      </HBox>

      <!-- Bandeau d'identité du passage -->
      <HBox spacing="28.0" styleClass="bandeau">
        <VBox spacing="2.0">
          <Label text="DATE / PLAGE" styleClass="bandeau-label"/>
          <Label fx:id="lblPlageHoraire" styleClass="bandeau-valeur"/>
        </VBox>
        <VBox spacing="2.0">
          <Label text="ENREGISTREUR" styleClass="bandeau-label"/>
          <Label fx:id="lblEnregistreur" styleClass="bandeau-valeur"/>
        </VBox>
        <VBox spacing="2.0">
          <Label text="STATUT" styleClass="bandeau-label"/>
          <Label fx:id="lblStatut" styleClass="bandeau-valeur"/>
        </VBox>
        <VBox spacing="2.0">
          <Label text="VERDICT" styleClass="bandeau-label"/>
          <Label fx:id="lblVerdict" styleClass="bandeau-valeur"/>
        </VBox>
      </HBox>

      <!-- Stepper de statut workflow -->
      <VBox spacing="4.0">
        <Label text="Statut du workflow" styleClass="section-sous"/>
        <HBox fx:id="stepper" spacing="8.0" styleClass="stepper"/>
      </VBox>

      <Label fx:id="lblMessage" styleClass="message-erreur" wrapText="true"/>
    </VBox>
  </top>

  <center>
    <VBox spacing="16.0" styleClass="hub-contenu">

      <!-- Résumé de la nuit -->
      <Label text="Résumé de la nuit" styleClass="section-sous"/>
      <HBox spacing="14.0" styleClass="stats">
        <VBox spacing="2.0" styleClass="stat-carte" maxWidth="Infinity" HBox.hgrow="ALWAYS">
          <Label text="VOLUME BRUTS" styleClass="stat-label"/>
          <Label fx:id="lblVolBruts" styleClass="stat-valeur"/>
        </VBox>
        <VBox spacing="2.0" styleClass="stat-carte" maxWidth="Infinity" HBox.hgrow="ALWAYS">
          <Label text="VOLUME TRANSFORMÉ" styleClass="stat-label"/>
          <Label fx:id="lblVolTransformes" styleClass="stat-valeur"/>
        </VBox>
        <VBox spacing="2.0" styleClass="stat-carte" maxWidth="Infinity" HBox.hgrow="ALWAYS">
          <Label text="DURÉE AUDIBLE" styleClass="stat-label"/>
          <Label fx:id="lblDureeAudible" styleClass="stat-valeur"/>
        </VBox>
        <VBox spacing="2.0" styleClass="stat-carte" maxWidth="Infinity" HBox.hgrow="ALWAYS">
          <Label text="SÉQUENCES" styleClass="stat-label"/>
          <Label fx:id="lblNbSequences" styleClass="stat-valeur"/>
        </VBox>
      </HBox>

      <!-- Conditions météo (#106 étendu) : grandeurs optionnelles, éditables (vide pour effacer). -->
      <Label text="Conditions météo (optionnel)" styleClass="section-sous"/>
      <HBox spacing="10.0" alignment="CENTER_LEFT">
        <Label text="Température début :"/>
        <TextField fx:id="champTemperature" promptText="ex. 8,5" prefWidth="80.0"/>
        <Label text="°C"/>
        <Label text="fin :"/>
        <TextField fx:id="champTemperatureFin" promptText="ex. 5,0" prefWidth="80.0"/>
        <Label text="°C"/>
      </HBox>
      <HBox spacing="10.0" alignment="CENTER_LEFT">
        <Label text="Vent :"/>
        <TextField fx:id="champVent" promptText="ex. 12" prefWidth="80.0"/>
        <Label text="km/h"/>
        <Label text="Couverture nuageuse :"/>
        <TextField fx:id="champCouverture" promptText="ex. 40" prefWidth="80.0"/>
        <Label text="\%"/>
      </HBox>
      <HBox spacing="10.0" alignment="CENTER_LEFT">
        <Button fx:id="boutonRecupererMeteo" text="Récupérer la météo" onAction="#recupererMeteo"/>
        <Button fx:id="boutonMeteo" text="Enregistrer" onAction="#enregistrerMeteo"/>
      </HBox>

      <!-- Matériel du micro déployé (dépôt VigieChiro) : grandeurs optionnelles. -->
      <Label text="Matériel du micro (optionnel)" styleClass="section-sous"/>
      <HBox spacing="10.0" alignment="CENTER_LEFT">
        <Label text="Position :"/>
        <ComboBox fx:id="champPosition" prefWidth="130.0"/>
        <Label text="Hauteur :"/>
        <TextField fx:id="champHauteur" promptText="ex. 4" prefWidth="70.0"/>
        <Label text="m"/>
        <Label text="Type de micro :"/>
        <TextField fx:id="champTypeMicro" promptText="ex. SM4 externe" prefWidth="150.0"/>
      </HBox>
      <HBox spacing="10.0" alignment="CENTER_LEFT">
        <Button fx:id="boutonMateriel" text="Enregistrer" onAction="#enregistrerMateriel"/>
      </HBox>

      <!-- Actions : avancer dans le workflow (le retour et le fil d'Ariane sont portés par le chrome) -->
      <Label text="Actions" styleClass="section-sous"/>
      <HBox spacing="16.0" fillHeight="true" styleClass="cartes-actions">
        <Button fx:id="boutonVerifier" styleClass="carte-action"
                onAction="#verifier" contentDisplay="GRAPHIC_ONLY"
                accessibleText="Vérifier l'enregistrement : sound check par échantillonnage"
                maxWidth="Infinity" maxHeight="Infinity" HBox.hgrow="ALWAYS">
          <graphic>
            <VBox alignment="CENTER" spacing="6.0" styleClass="carte-action-contenu">
              <FontIcon iconLiteral="fas-headphones" styleClass="carte-action-icone"/>
              <Label text="Vérifier l'enregistrement" styleClass="carte-action-titre" wrapText="true"/>
              <Label text="Sound check par échantillonnage" styleClass="carte-action-sous" wrapText="true"/>
            </VBox>
          </graphic>
        </Button>
        <Button fx:id="boutonDiagnostic" styleClass="carte-action"
                onAction="#diagnostiquer" contentDisplay="GRAPHIC_ONLY"
                accessibleText="Diagnostic matériel : température, hygrométrie, anomalies, journal"
                maxWidth="Infinity" maxHeight="Infinity" HBox.hgrow="ALWAYS">
          <graphic>
            <VBox alignment="CENTER" spacing="6.0" styleClass="carte-action-contenu">
              <FontIcon iconLiteral="fas-tools" styleClass="carte-action-icone"/>
              <Label text="Diagnostic matériel" styleClass="carte-action-titre" wrapText="true"/>
              <Label text="T°/hygrométrie, anomalies, journal" styleClass="carte-action-sous" wrapText="true"/>
            </VBox>
          </graphic>
        </Button>
        <Button fx:id="boutonDepot" styleClass="carte-action"
                onAction="#preparerDepot" contentDisplay="GRAPHIC_ONLY"
                accessibleText="Préparer le dépôt : constituer le lot Tadarida"
                maxWidth="Infinity" maxHeight="Infinity" HBox.hgrow="ALWAYS">
          <graphic>
            <VBox alignment="CENTER" spacing="6.0" styleClass="carte-action-contenu">
              <FontIcon iconLiteral="fas-box-open" styleClass="carte-action-icone"/>
              <Label text="Préparer le dépôt" styleClass="carte-action-titre" wrapText="true"/>
              <Label text="Constituer le lot Tadarida" styleClass="carte-action-sous" wrapText="true"/>
            </VBox>
          </graphic>
        </Button>
        <Button fx:id="boutonValidation" styleClass="carte-action"
                onAction="#validerTadarida" contentDisplay="GRAPHIC_ONLY"
                accessibleText="Validation Tadarida : valider les identifications"
                maxWidth="Infinity" maxHeight="Infinity" HBox.hgrow="ALWAYS">
          <graphic>
            <VBox alignment="CENTER" spacing="6.0" styleClass="carte-action-contenu">
              <FontIcon iconLiteral="fas-check-circle" styleClass="carte-action-icone"/>
              <Label text="Validation Tadarida" styleClass="carte-action-titre" wrapText="true"/>
              <Label text="Valider les identifications" styleClass="carte-action-sous" wrapText="true"/>
            </VBox>
          </graphic>
        </Button>
        <Button fx:id="boutonNonIdentifies" styleClass="carte-action"
                onAction="#ecouterNonIdentifies" contentDisplay="GRAPHIC_ONLY"
                accessibleText="Écouter les non identifiés : sons absents du CSV Tadarida, à valider à la main"
                maxWidth="Infinity" maxHeight="Infinity" HBox.hgrow="ALWAYS">
          <graphic>
            <VBox alignment="CENTER" spacing="6.0" styleClass="carte-action-contenu">
              <FontIcon iconLiteral="fas-question-circle" styleClass="carte-action-icone"/>
              <Label text="Écouter les non identifiés" styleClass="carte-action-titre" wrapText="true"/>
              <Label text="Sons absents du CSV Tadarida" styleClass="carte-action-sous" wrapText="true"/>
            </VBox>
          </graphic>
        </Button>
      </HBox>
      <Label fx:id="lblIndiceAction" styleClass="section-sous" wrapText="true"/>
    </VBox>
  </center>
</BorderPane>

